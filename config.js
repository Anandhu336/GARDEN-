/* Connect Keepsake to your free Supabase project (steps are in SETUP.md).
   Both values are safe to publish: the "anon / publishable" key is meant to be public.
   Never paste the "service_role" or "secret" key here. */
window.KS_CONFIG = {
  SUPABASE_URL: '',       // e.g. https://abcdxyz.supabase.co
  SUPABASE_ANON_KEY: ''   // the anon / publishable key
};
